package models.pages;

import models.MemoryState;

// IRRELEVANT - This is essentially the same thing as MemoryState
public class ProcessFrameTable extends MemoryState {
	// Shows the mapping of physical memory at a given state
}
